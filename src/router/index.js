import Vue from 'vue'
import Router from 'vue-router'
import Results from './../components/TimeHeader.vue'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
      name: 'Main'
    },
  ]
})
